/* function deleteNote(noteId) {
    fexth('/delete-note', {
        method: "POST",
        body: JSON.stringify({ noteId: noteId }),
    }).then((_res) => {
        window.location.href ="/"
    })
} 

Não será utilizado o js que está aqui, 
será utilizado o que está no arquivo base.html
*/